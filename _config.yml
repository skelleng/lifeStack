title: LifeStack
description: "A modular framework for intentional living: Cook, Clean, Build."
author: skelleng
url: "https://skelleng.github.io"
baseurl: "/lifeStack"
theme: jekyll-theme-minimal
markdown: kramdown
sass:
  style: compressed
collections:
  cook:
    output: true
    permalink: /cook/:year/:month/:day/:title/
  clean:
    output: true
    permalink: /clean/:year/:month/:day/:title/
  build:
    output: true
    permalink: /build/:year/:month/:day/:title/

defaults:
  - scope:
      path: ""
      type: cook
    values:
      layout: post
  - scope:
      path: ""
      type: clean
    values:
      layout: post
  - scope:
      path: ""
      type: build
    values:
      layout: post


plugins:
  - jekyll-sitemap
  - jekyll-seo-tag
  - jekyll-feed
